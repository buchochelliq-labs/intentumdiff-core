name: CI

on:
  push:
    branches: ["main"]
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  engine:
    name: Engine pins (cargo test)
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - name: Install Rust 1.93.0
        uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # stable
        with:
          toolchain: 1.93.0
      - name: Cache Rust build artefacts
        uses: Swatinem/rust-cache@82a92a6e8fbeee089604da2575dc567ae9ddeaab # v2.7.5
        with:
          key: engine
          workspaces: crates/rust-core-host
      - name: Run engine pins
        # Standalone crate (not a workspace member) — run from the crate directory.
        # --no-default-features skips the 25 tier-c-wasm tests that need staged parser
        # components (built by the parser repos): Phase D wires cross-repo component
        # provisioning (INTENTDIFF_TEST_WASM_DIR), then this flips back to the full gate.
        run: cargo test --no-default-features
        working-directory: crates/rust-core-host

  cli:
    name: Native CLI builds
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - name: Install Rust 1.93.0
        uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # stable
        with:
          toolchain: 1.93.0
      - name: Cache Rust build artefacts
        uses: Swatinem/rust-cache@82a92a6e8fbeee089604da2575dc567ae9ddeaab # v2.7.5
        with:
          key: cli
          workspaces: crates/cli
      - name: Build the intentdiff CLI
        run: cargo build --release
        working-directory: crates/cli

  components:
    name: Wasm components build (index engine + renderers)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - name: Install Rust 1.93.0 (+ wasm32-wasip2)
        uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # stable
        with:
          toolchain: 1.93.0
          targets: wasm32-wasip2
      - name: Cache Rust build artefacts
        uses: Swatinem/rust-cache@82a92a6e8fbeee089604da2575dc567ae9ddeaab # v2.7.5
        with:
          key: components
      - name: Build the workspace components
        run: cargo build --release --target wasm32-wasip2
