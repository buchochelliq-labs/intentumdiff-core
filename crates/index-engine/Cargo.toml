[package]
name        = "intentdiff-index-engine"
version     = "0.1.0"
edition     = "2021"
description = "Cross-file semantic index engine plugin for IntentDiff"
license     = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
intentdiff-plugin-sdk = { git = "https://github.com/buchochelliq-labs/intentdiff-plugin-sdk", tag = "v0.1.0" }
index-engine-lib = { path = "../index-engine-lib" }
wit-bindgen  = { version = "0.57", default-features = false, features = ["std", "macros"] }

[profile.release-wasm]
inherits  = "release"
opt-level = "z"
lto       = true
strip     = true
