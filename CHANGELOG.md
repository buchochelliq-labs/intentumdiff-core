# Changelog

## v0.1.0 — 2026-07-26

Initial import from the IntentDiff monorepo (files-only; the monorepo remains the archive of
record). The complete engine: semantic diff pipeline across 69 languages, the stable C ABI
(`intentdiff_call`), the native `intentdiff` CLI, the index engine, and the four renderer
components. Engine pin suite 203/203.
